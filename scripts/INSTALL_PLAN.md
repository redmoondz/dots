# Superseded

Этот дизайн-док реализован. Установщик — `install.py` в корне репо (см. `README.md`),
манифест — `packages.json`, бутстрап для голой системы — `install.sh`.

`scripts/sync_config.sh` заменён командой `python3 install.py sync`.
